{
  "name": "nexamind-clinical",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Prototipo React de seguimiento farmacoterapéutico y agenda clínica para psiquiatría.",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 4173",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173",
    "check": "node --check src/app.js && node --check src/data.js && node --check src/utils.js && vite build"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "dependencies": {
    "htm": "3.1.1",
    "react": "19.2.7",
    "react-dom": "19.2.7"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
